# Execution (P3 paper/shadow pending)

Only paper OMS and shadow decisions belong here before G3. Any future venue adapter must expose order
intent, acknowledgement, partial fill, fill, cancel, reject, and unknown states through the same state
machine used by replay.

There is no mainnet order submission code in the repository today.
