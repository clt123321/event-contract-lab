# Research (P2 pending)

Formal experiments will reference immutable dataset manifests and point-in-time features. A result is not
reproducible unless it records the code commit, schema/config versions, quality mask, random seed, fee,
latency, slippage, and settlement assumptions. Failed experiments are retained.
